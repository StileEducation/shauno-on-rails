<h1>Button "<%= @button.uuid %>" at <%= @event.timestamp %></h1>

<p>
    <strong>Reason: </strong><%= @reason.reason %>
    <br>
    <strong>Developer: </strong> <%= @developer.name %>
</p>

<%= link_to "Ignore this event", edit_event_path, data: {confirm: "Are you sure?"} %>
<br>
<%= link_to "Back to all events", events_path%>